import { createActionType } from 'base';

export const ActionTypes = new Map<string, string>([
  ['LAZY_ERROR', 'LAZY_ERROR'],
  ['LAZY_REQUEST', 'LAZY_REQUEST'],
  ['LAZY_SUCCESS', 'LAZY_SUCCESS']
]);
