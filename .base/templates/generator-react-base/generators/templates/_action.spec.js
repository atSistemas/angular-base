import { expect } from 'chai';
import * as Actions from '../';
import Types from '../../types';
import { generateFetchTypes } from 'base/shared/TypeHelper';

import api from '../../api';

describe('Actions', () => {
  describe('<%= _.capitalize(name) %> Actions', () => {

    it('Should create query action', () => {

    });

  });
});
